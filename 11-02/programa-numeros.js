/*
    - verifica se o numero e possitivo ou negativo ou zero
    se o numero for menor que o zero , é negativo
    se o numero for maior que zero e possitivo
    Senão o numero não e valido
*/ 


let numero = prompt("digite um numero")

if ( numero > 0 ) {
    document.write('o numero ${} e possitivo')

}else if (numero < 0 ) {
    document.write( '')
}
